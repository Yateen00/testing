services:
  app:
    image: nginx:alpine
    container_name: test_webhook_app
    volumes:
      - ./message.txt:/usr/share/nginx/html/index.html:ro
    ports:
      - "5010:85"
    restart: unless-stopped
